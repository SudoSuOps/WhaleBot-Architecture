import { ClosedTrade, Position } from '../types';
export const sendDiscordAlert = async (webhookUrl: string, type: 'OPEN' | 'CLOSE' | 'TP' | 'SL' | 'BOT', data: any) => { if (!webhookUrl) return; const payload = { username: "WhaleBot v3.4", content: "Trade Alert" }; try { await fetch(webhookUrl, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(payload) }); } catch (e) { console.error("Discord Webhook Error", e); } };
